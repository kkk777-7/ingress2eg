apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-tls-ingress1
  namespace: namespace1
  annotations:
    nginx.ingress.kubernetes.io/proxy-ssl-secret: "namespace1/client-cert"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "backend.example.com"
spec:
  ingressClassName: nginx
  rules:
  - host: example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service1
            port:
              number: 443
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-tls-ingress2
  namespace: namespace1
  annotations:
    nginx.ingress.kubernetes.io/proxy-ssl-secret: "namespace1/client-cert"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "backend.example.com"
spec:
  ingressClassName: nginx
  rules:
  - host: example.com
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: backend-service2
            port:
              number: 443
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service1
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 443
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service2
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 443
