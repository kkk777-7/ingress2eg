apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: production-ingress
  namespace: namespace1
spec:
  ingressClassName: nginx
  rules:
  - host: example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: production-api-service
            port:
              number: 80
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: production-admin-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canary-api-ingress
  namespace: namespace1
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "20"
spec:
  ingressClassName: nginx
  rules:
  - host: example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: canary-api-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canary-admin-ingress
  namespace: namespace1
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "10"
spec:
  ingressClassName: nginx
  rules:
  - host: example.com
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: canary-admin-service
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: production-api-service
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: production-admin-service
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: canary-api-service
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: canary-admin-service
  namespace: namespace1
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
